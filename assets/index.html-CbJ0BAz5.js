import{_ as t,c as a,b as o,o as i}from"./app-Cvez0BgB.js";const r="/assets/image-20260423210250685-opTEYNea.png",d="/assets/image-20260423210346879-CwzFMCT9.png",s="/assets/image-20260423210504213-BHIMB9v5.png",n="/assets/image-20260423210602628-B-0nVxVJ.png",l={};function c(h,e){return i(),a("div",null,e[0]||(e[0]=[o('<p>Aggregation tables are used to improve query performance in Datafor.</p><p>Instead of aggregating data from the detail fact table every time a report runs, Datafor can use a pre-aggregated table when the report query matches the granularity and metric definitions of that table. This can reduce scanned data volume, shorten SQL execution time, and improve dashboard responsiveness.</p><h2 id="what-is-an-aggregation-table" tabindex="-1"><a class="header-anchor" href="#what-is-an-aggregation-table"><span>What is an aggregation table</span></a></h2><p>A fact table usually stores data at the lowest business granularity, such as one row per transaction or one row per sales record.</p><p>However, many reports do not need that level of detail. For example, users often analyze:</p><ul><li>sales by year</li><li>sales by quarter</li><li>sales by store</li><li>sales by product category</li></ul><p>If these queries are frequent and the fact table is large, repeatedly aggregating detail data can become expensive.</p><p>An aggregation table stores pre-calculated results at a higher level of granularity, such as:</p><ul><li>by store and year</li><li>by store and quarter</li><li>by product and month</li><li>by region and year</li></ul><p>When a report query is compatible with the aggregation table, Datafor can use that table instead of the detail fact table.</p><h2 id="benefits-of-aggregation-tables" tabindex="-1"><a class="header-anchor" href="#benefits-of-aggregation-tables"><span>Benefits of aggregation tables</span></a></h2><p>Aggregation tables are mainly used for performance optimization.</p><p>Typical benefits include:</p><ul><li>faster report execution</li><li>lower SQL cost</li><li>less database load</li><li>better user experience for dashboards and charts</li><li>better performance for large fact tables</li></ul><p>Aggregation tables are not a replacement for detail tables. They are an optimization layer for common analysis patterns.</p><h2 id="when-to-use-aggregation-tables" tabindex="-1"><a class="header-anchor" href="#when-to-use-aggregation-tables"><span>When to use aggregation tables</span></a></h2><p>Aggregation tables are recommended when:</p><ul><li>the fact table contains a large amount of data</li><li>users frequently run similar summary queries</li><li>dashboards repeatedly use the same dimensions and measures</li><li>report response time needs to be improved</li><li>the analysis granularity is relatively stable</li></ul><p>For example, aggregation tables are especially useful when users often analyze:</p><ul><li>sales by month, quarter, or year</li><li>sales by store</li><li>sales by product</li><li>cost by region</li><li>unit sales by time period</li></ul><h2 id="before-you-begin" tabindex="-1"><a class="header-anchor" href="#before-you-begin"><span>Before you begin</span></a></h2><p>Before configuring an aggregation table in Datafor, make sure the following are ready:</p><h3 id="_1-the-aggregation-table-already-exists-in-the-database" tabindex="-1"><a class="header-anchor" href="#_1-the-aggregation-table-already-exists-in-the-database"><span>1. The aggregation table already exists in the database</span></a></h3><p>Datafor maps an existing aggregation table to the detail model. It does not create the aggregation table automatically.</p><p>Examples:</p><ul><li><code>agg_sales_store_month</code></li><li><code>agg_sales_region_quarter</code></li><li><code>agg_product_year</code></li><li><code>agg_c_special_sales_fact_1997</code></li></ul><h3 id="_2-the-granularity-of-the-aggregation-table-is-clear" tabindex="-1"><a class="header-anchor" href="#_2-the-granularity-of-the-aggregation-table-is-clear"><span>2. The granularity of the aggregation table is clear</span></a></h3><p>You should know exactly what the table is grouped by.</p><p>Examples:</p><ul><li>store + year + quarter</li><li>product + month</li><li>region + year</li></ul><p>This is important because only compatible queries can use the aggregation table.</p><h3 id="_3-the-measures-are-clearly-defined" tabindex="-1"><a class="header-anchor" href="#_3-the-measures-are-clearly-defined"><span>3. The measures are clearly defined</span></a></h3><p>You should know how each aggregated measure in the table was calculated.</p><p>Examples:</p><ul><li><code>store_sales_sum</code> = <code>SUM(store_sales)</code></li><li><code>unit_sales_sum</code> = <code>SUM(unit_sales)</code></li><li><code>fact_count</code> = number of detail rows rolled into the aggregated row</li></ul><h2 id="open-the-aggregation-configuration" tabindex="-1"><a class="header-anchor" href="#open-the-aggregation-configuration"><span>Open the aggregation configuration</span></a></h2><p>In the modeling canvas, select a table, open the table menu, and click <strong>Manage aggregations</strong>.</p><div align="left"><img src="'+r+'" width="85%"></div><p>The <strong>Manage aggregations</strong> dialog is used to map aggregation table columns to fields in the detail model.</p><div align="left"><img src="'+d+'" width="85%"></div><h2 id="fields-in-the-manage-aggregations-dialog" tabindex="-1"><a class="header-anchor" href="#fields-in-the-manage-aggregations-dialog"><span>Fields in the Manage aggregations dialog</span></a></h2><p>The dialog contains the following columns:</p><table><thead><tr><th>Column</th><th>Description</th></tr></thead><tbody><tr><td><strong>Aggregation column</strong></td><td>A column in the aggregation table</td></tr><tr><td><strong>Summarization</strong></td><td>The aggregation behavior that explains how this aggregation column is derived from the detail model</td></tr><tr><td><strong>Detail table</strong></td><td>The table in the detail model that the aggregation column maps to</td></tr><tr><td><strong>Detail column</strong></td><td>The field in the detail table that the aggregation column maps to</td></tr></tbody></table><p>Each row defines one mapping between the aggregation table and the detail model.</p><h2 id="how-to-configure-an-aggregation-table" tabindex="-1"><a class="header-anchor" href="#how-to-configure-an-aggregation-table"><span>How to configure an aggregation table</span></a></h2><p>For each aggregation table column, choose the correct summarization method and map it to the corresponding detail field.</p><h3 id="example" tabindex="-1"><a class="header-anchor" href="#example"><span>Example</span></a></h3><p>Suppose the aggregation table is grouped by:</p><ul><li><code>store_id</code></li><li><code>time_year</code></li><li><code>time_quarter</code></li></ul><p>And it stores the following aggregated measures:</p><ul><li><code>store_sales_sum</code></li><li><code>unit_sales_sum</code></li></ul><p>A typical configuration would be:</p><table><thead><tr><th>Aggregation column</th><th>Summarization</th><th>Detail table</th><th>Detail column</th></tr></thead><tbody><tr><td><code>store_id</code></td><td><code>GroupBy</code></td><td><code>sales_fact</code></td><td><code>store_id</code></td></tr><tr><td><code>time_year</code></td><td><code>GroupBy</code></td><td><code>time_by_day</code></td><td><code>the_year</code></td></tr><tr><td><code>time_quarter</code></td><td><code>GroupBy</code></td><td><code>time_by_day</code></td><td><code>quarter</code></td></tr><tr><td><code>store_sales_sum</code></td><td><code>Sum</code></td><td><code>sales_fact</code></td><td><code>store_sales</code></td></tr><tr><td><code>unit_sales_sum</code></td><td><code>Sum</code></td><td><code>sales_fact</code></td><td><code>unit_sales</code></td></tr></tbody></table><p>This tells Datafor that:</p><ul><li><code>store_id</code>, <code>time_year</code>, and <code>time_quarter</code> define the grouping level</li><li><code>store_sales_sum</code> is the sum of <code>sales_fact.store_sales</code></li><li><code>unit_sales_sum</code> is the sum of <code>sales_fact.unit_sales</code></li></ul><h2 id="summarization-methods" tabindex="-1"><a class="header-anchor" href="#summarization-methods"><span>Summarization methods</span></a></h2><p>The <strong>Summarization</strong> field tells Datafor how an aggregation table column was derived from the detail model.</p><p>The available options are:</p><ul><li><code>GroupBy</code></li><li><code>Sum</code></li><li><code>Count</code></li><li><code>Max</code></li><li><code>Min</code></li><li><code>Count table</code></li></ul><p>Choosing the correct summarization method is critical. If it is wrong, Datafor may not use the aggregation table, or the query result may be incorrect.</p><h3 id="groupby" tabindex="-1"><a class="header-anchor" href="#groupby"><span>GroupBy</span></a></h3><p>Use <code>GroupBy</code> when the aggregation column is a grouping key, not a calculated metric.</p><p>Typical examples:</p><ul><li><code>store_id</code></li><li><code>product_id</code></li><li><code>customer_id</code></li><li><code>time_year</code></li><li><code>time_quarter</code></li><li><code>time_month</code></li><li><code>region_id</code></li></ul><p>Example:</p><table><thead><tr><th>Aggregation column</th><th>Summarization</th><th>Detail table</th><th>Detail column</th></tr></thead><tbody><tr><td><code>store_id</code></td><td><code>GroupBy</code></td><td><code>sales_fact</code></td><td><code>store_id</code></td></tr><tr><td><code>time_quarter</code></td><td><code>GroupBy</code></td><td><code>time_by_day</code></td><td><code>quarter</code></td></tr></tbody></table><p>Choose <code>GroupBy</code> when the column represents a dimension that defines the aggregation grain.</p><h3 id="sum" tabindex="-1"><a class="header-anchor" href="#sum"><span>Sum</span></a></h3><p>Use <code>Sum</code> when the aggregation column stores the result of <code>SUM()</code> on a detail field.</p><p>Typical examples:</p><ul><li>sales amount</li><li>cost amount</li><li>quantity</li><li>unit sales</li></ul><p>Example:</p><table><thead><tr><th>Aggregation column</th><th>Summarization</th><th>Detail table</th><th>Detail column</th></tr></thead><tbody><tr><td><code>store_sales_sum</code></td><td><code>Sum</code></td><td><code>sales_fact</code></td><td><code>store_sales</code></td></tr><tr><td><code>unit_sales_sum</code></td><td><code>Sum</code></td><td><code>sales_fact</code></td><td><code>unit_sales</code></td></tr><tr><td><code>store_cost_sum</code></td><td><code>Sum</code></td><td><code>sales_fact</code></td><td><code>store_cost</code></td></tr></tbody></table><p>Choose <code>Sum</code> when the metric is additive and can be safely rolled up across higher levels.</p><h3 id="count" tabindex="-1"><a class="header-anchor" href="#count"><span>Count</span></a></h3><p>Use <code>Count</code> when the aggregation column stores the result of <code>COUNT()</code> for a detail field.</p><p>Typical examples:</p><ul><li>order count</li><li>customer count</li><li>number of non-null values in a field</li></ul><p>Example:</p><table><thead><tr><th>Aggregation column</th><th>Summarization</th><th>Detail table</th><th>Detail column</th></tr></thead><tbody><tr><td><code>order_count</code></td><td><code>Count</code></td><td><code>sales_fact</code></td><td><code>order_id</code></td></tr></tbody></table><p>Choose <code>Count</code> when the metric is the count of a specific field.</p><h3 id="max" tabindex="-1"><a class="header-anchor" href="#max"><span>Max</span></a></h3><p>Use <code>Max</code> when the aggregation column stores the result of <code>MAX()</code> for a detail field.</p><p>Typical examples:</p><ul><li>highest price</li><li>latest date</li><li>maximum discount</li></ul><p>Example:</p><table><thead><tr><th>Aggregation column</th><th>Summarization</th><th>Detail table</th><th>Detail column</th></tr></thead><tbody><tr><td><code>max_price</code></td><td><code>Max</code></td><td><code>sales_fact</code></td><td><code>unit_price</code></td></tr></tbody></table><p>Choose <code>Max</code> when the metric represents the maximum value at the aggregation grain.</p><h3 id="min" tabindex="-1"><a class="header-anchor" href="#min"><span>Min</span></a></h3><p>Use <code>Min</code> when the aggregation column stores the result of <code>MIN()</code> for a detail field.</p><p>Typical examples:</p><ul><li>lowest price</li><li>earliest date</li><li>minimum inventory value</li></ul><p>Example:</p><table><thead><tr><th>Aggregation column</th><th>Summarization</th><th>Detail table</th><th>Detail column</th></tr></thead><tbody><tr><td><code>min_price</code></td><td><code>Min</code></td><td><code>sales_fact</code></td><td><code>unit_price</code></td></tr></tbody></table><p>Choose <code>Min</code> when the metric represents the minimum value at the aggregation grain.</p><h3 id="count-table" tabindex="-1"><a class="header-anchor" href="#count-table"><span>Count table</span></a></h3><p>Use <code>Count table</code> when the aggregation column stores the number of detail fact rows represented by the aggregated row.</p><p>Typical examples:</p><ul><li><code>fact_count</code></li><li><code>row_count</code></li><li><code>record_count</code></li></ul><p>This is commonly used when the aggregation table stores how many fact table rows were rolled up into each aggregate row.</p><p>Example:</p><table><thead><tr><th>Aggregation column</th><th>Summarization</th><th>Detail table</th><th>Detail column</th></tr></thead><tbody><tr><td><code>fact_count</code></td><td><code>Count table</code></td><td><code>sales_fact</code></td><td></td></tr></tbody></table><p>Choose <code>Count table</code> when the column represents the count of underlying fact rows rather than the count of one specific detail field.</p><blockquote><p>If you are not sure whether a count-related column should use <code>Count</code> or <code>Count table</code>, first confirm the exact meaning of that column in the database.</p></blockquote><h2 id="how-to-choose-the-correct-summarization-method" tabindex="-1"><a class="header-anchor" href="#how-to-choose-the-correct-summarization-method"><span>How to choose the correct summarization method</span></a></h2><p>A simple way to choose is to ask:</p><p><strong>Is this column a grouping key, or is it a calculated aggregate result?</strong></p><h3 id="choose-groupby-for-dimension-keys" tabindex="-1"><a class="header-anchor" href="#choose-groupby-for-dimension-keys"><span>Choose <code>GroupBy</code> for dimension keys</span></a></h3><p>If the column defines the aggregation grain, use <code>GroupBy</code>.</p><p>Examples:</p><ul><li><code>store_id</code></li><li><code>product_id</code></li><li><code>customer_id</code></li><li><code>time_year</code></li><li><code>time_quarter</code></li><li><code>time_month</code></li></ul><h3 id="choose-sum-for-additive-metrics" tabindex="-1"><a class="header-anchor" href="#choose-sum-for-additive-metrics"><span>Choose <code>Sum</code> for additive metrics</span></a></h3><p>If the column stores a total amount or total quantity, use <code>Sum</code>.</p><p>Examples:</p><ul><li><code>store_sales_sum</code></li><li><code>unit_sales_sum</code></li><li><code>store_cost_sum</code></li></ul><h3 id="choose-count-or-count-table-for-count-metrics" tabindex="-1"><a class="header-anchor" href="#choose-count-or-count-table-for-count-metrics"><span>Choose <code>Count</code> or <code>Count table</code> for count metrics</span></a></h3><p>If the column stores a count:</p><ul><li>use <code>Count</code> when it counts a specific field</li><li>use <code>Count table</code> when it counts fact table rows</li></ul><p>Examples:</p><table><thead><tr><th>Aggregation column</th><th>Recommended summarization</th></tr></thead><tbody><tr><td><code>order_count</code></td><td><code>Count</code></td></tr><tr><td><code>customer_count</code></td><td><code>Count</code></td></tr><tr><td><code>fact_count</code></td><td><code>Count table</code></td></tr></tbody></table><h3 id="choose-max-or-min-for-extreme-values" tabindex="-1"><a class="header-anchor" href="#choose-max-or-min-for-extreme-values"><span>Choose <code>Max</code> or <code>Min</code> for extreme values</span></a></h3><p>If the column stores a highest or lowest value, use <code>Max</code> or <code>Min</code>.</p><p>Examples:</p><table><thead><tr><th>Aggregation column</th><th>Recommended summarization</th></tr></thead><tbody><tr><td><code>max_discount</code></td><td><code>Max</code></td></tr><tr><td><code>min_price</code></td><td><code>Min</code></td></tr></tbody></table><h2 id="quick-selection-guide" tabindex="-1"><a class="header-anchor" href="#quick-selection-guide"><span>Quick selection guide</span></a></h2><p>You can use the following rule of thumb:</p><ul><li>dimension field -&gt; <code>GroupBy</code></li><li>additive metric -&gt; <code>Sum</code></li><li>count metric -&gt; <code>Count</code> or <code>Count table</code></li><li>highest value -&gt; <code>Max</code></li><li>lowest value -&gt; <code>Min</code></li></ul><h2 id="example-based-on-the-current-aggregation-table" tabindex="-1"><a class="header-anchor" href="#example-based-on-the-current-aggregation-table"><span>Example based on the current aggregation table</span></a></h2><p>For the aggregation table shown in the example, a reasonable configuration would be:</p><table><thead><tr><th>Aggregation column</th><th>Recommended summarization</th><th>Notes</th></tr></thead><tbody><tr><td><code>store_id</code></td><td><code>GroupBy</code></td><td>grouped by store</td></tr><tr><td><code>time_year</code></td><td><code>GroupBy</code></td><td>grouped by year</td></tr><tr><td><code>time_quarter</code></td><td><code>GroupBy</code></td><td>grouped by quarter</td></tr><tr><td><code>time_month</code></td><td><code>GroupBy</code> if month is part of the aggregation grain</td><td>otherwise leave unmapped</td></tr><tr><td><code>store_sales_sum</code></td><td><code>Sum</code></td><td>total store sales</td></tr><tr><td><code>unit_sales_sum</code></td><td><code>Sum</code></td><td>total unit sales</td></tr><tr><td><code>store_cost_sum</code></td><td><code>Sum</code></td><td>total store cost</td></tr><tr><td><code>fact_count</code></td><td><code>Count table</code> or <code>Count</code></td><td>depends on the actual meaning of the column</td></tr><tr><td><code>customer_id</code></td><td><code>GroupBy</code> only if customer is part of the aggregation grain</td><td>otherwise leave unmapped</td></tr><tr><td><code>product_id</code></td><td><code>GroupBy</code> only if product is part of the aggregation grain</td><td>otherwise leave unmapped</td></tr><tr><td><code>promotion_id</code></td><td><code>GroupBy</code> only if promotion is part of the aggregation grain</td><td>otherwise leave unmapped</td></tr></tbody></table><h2 id="how-datafor-decides-whether-to-use-an-aggregation-table" tabindex="-1"><a class="header-anchor" href="#how-datafor-decides-whether-to-use-an-aggregation-table"><span>How Datafor decides whether to use an aggregation table</span></a></h2><p>After the aggregation table is configured, Datafor will only use it when the report query is compatible with the table.</p><p>In general, the following conditions must be satisfied.</p><h3 id="_1-the-query-grain-must-be-compatible" tabindex="-1"><a class="header-anchor" href="#_1-the-query-grain-must-be-compatible"><span>1. The query grain must be compatible</span></a></h3><p>The report query cannot require a finer grain than the aggregation table.</p><p>Example:</p><ul><li>aggregation table grain: store + year + quarter</li><li>report query grain: store + month</li></ul><p>Because month is more detailed than quarter, the query usually cannot use that aggregation table.</p><h3 id="_2-the-requested-measures-must-be-compatible" tabindex="-1"><a class="header-anchor" href="#_2-the-requested-measures-must-be-compatible"><span>2. The requested measures must be compatible</span></a></h3><p>The measures in the report must match the summarized metrics supported by the aggregation table.</p><p>For example, a table that only stores <code>SUM(store_sales)</code> cannot automatically answer every possible metric request, such as:</p><ul><li>distinct customer count</li><li>average basket size</li><li>ratio metrics</li><li>complex calculated measures</li></ul><h3 id="_3-filters-must-be-compatible" tabindex="-1"><a class="header-anchor" href="#_3-filters-must-be-compatible"><span>3. Filters must be compatible</span></a></h3><p>If the query uses filters that are not safely supported by the aggregation table grain, Datafor may fall back to the detail table.</p><h2 id="verify-whether-a-report-query-uses-the-aggregation-table" tabindex="-1"><a class="header-anchor" href="#verify-whether-a-report-query-uses-the-aggregation-table"><span>Verify whether a report query uses the aggregation table</span></a></h2><p>After configuration, the best way to confirm whether the aggregation table is being used is to inspect the SQL in the report editor.</p><p>In the report editor, select a chart, open the chart menu, and click <strong>Execution cost</strong>.</p><div align="left"><img src="'+s+'" width="85%"></div><p>The <strong>Execution cost</strong> dialog shows the generated SQL and execution time.</p><div align="left"><img src="'+n+'" width="85%"></div><h3 id="what-to-check-in-the-sql" tabindex="-1"><a class="header-anchor" href="#what-to-check-in-the-sql"><span>What to check in the SQL</span></a></h3><p>Look at the actual table names used in the SQL.</p><ul><li>If the SQL reads from the aggregation table, such as <code>agg_c_special_sales_fact_1997</code>, the aggregation table was used.</li><li>If the SQL still reads from the detail fact table, such as <code>sales_fact</code>, the aggregation table was not used for that query.</li></ul><h2 id="example-of-a-query-not-using-the-aggregation-table" tabindex="-1"><a class="header-anchor" href="#example-of-a-query-not-using-the-aggregation-table"><span>Example of a query not using the aggregation table</span></a></h2><p>If the SQL still uses:</p><ul><li><code>sales_fact</code></li><li><code>time_by_day</code></li><li><code>store</code></li></ul><p>instead of the aggregation table, the query did not match the aggregation table.</p><p>A common reason is grain mismatch.</p><p>For example:</p><ul><li>the aggregation table is grouped by year and quarter</li><li>the SQL groups by month, such as <code>to_char(the_date, &#39;yyyy-MM&#39;)</code></li></ul><p>Since month is more detailed than quarter, the query usually has to fall back to the detail table.</p><p>This is an important point:</p><p><strong>Configuring an aggregation table does not mean every report will automatically use it. The query must match the table grain and measure definitions.</strong></p><h2 id="best-practices" tabindex="-1"><a class="header-anchor" href="#best-practices"><span>Best practices</span></a></h2><h3 id="start-with-high-frequency-slow-queries" tabindex="-1"><a class="header-anchor" href="#start-with-high-frequency-slow-queries"><span>Start with high-frequency slow queries</span></a></h3><p>Do not build many aggregation tables at once. Start with the most common slow queries and optimize those first.</p><h3 id="make-the-grain-explicit" tabindex="-1"><a class="header-anchor" href="#make-the-grain-explicit"><span>Make the grain explicit</span></a></h3><p>Use clear table names that reflect the grain.</p><p>Examples:</p><ul><li><code>agg_sales_store_month</code></li><li><code>agg_sales_region_quarter</code></li><li><code>agg_sales_product_year</code></li></ul><h3 id="keep-the-measure-meaning-consistent" tabindex="-1"><a class="header-anchor" href="#keep-the-measure-meaning-consistent"><span>Keep the measure meaning consistent</span></a></h3><p>The aggregation table measure must match the semantic meaning used in reports.</p><h3 id="validate-with-sql" tabindex="-1"><a class="header-anchor" href="#validate-with-sql"><span>Validate with SQL</span></a></h3><p>Always verify configuration results in the report editor by checking the SQL and execution time.</p><h3 id="do-not-optimize-only-very-coarse-grains" tabindex="-1"><a class="header-anchor" href="#do-not-optimize-only-very-coarse-grains"><span>Do not optimize only very coarse grains</span></a></h3><p>If users often analyze data by month, a yearly aggregation table may provide limited benefit.</p><h3 id="keep-the-aggregation-table-refreshed" tabindex="-1"><a class="header-anchor" href="#keep-the-aggregation-table-refreshed"><span>Keep the aggregation table refreshed</span></a></h3><p>If the detail fact table changes but the aggregation table is not updated in time, report results may become inconsistent.</p><h2 id="design-recommendations" tabindex="-1"><a class="header-anchor" href="#design-recommendations"><span>Design recommendations</span></a></h2><h3 id="design-by-time-grain" tabindex="-1"><a class="header-anchor" href="#design-by-time-grain"><span>Design by time grain</span></a></h3><p>Create separate aggregation tables for:</p><ul><li>month</li><li>quarter</li><li>year</li></ul><p>This is useful when time-based analysis is common.</p><h3 id="design-by-subject-area" tabindex="-1"><a class="header-anchor" href="#design-by-subject-area"><span>Design by subject area</span></a></h3><p>Create aggregation tables for frequent analysis subjects, such as:</p><ul><li>by store</li><li>by product</li><li>by region</li></ul><h3 id="design-by-dashboard-use-case" tabindex="-1"><a class="header-anchor" href="#design-by-dashboard-use-case"><span>Design by dashboard use case</span></a></h3><p>For important dashboards, build aggregation tables that specifically match the chart patterns used there.</p><p>This is often the most practical strategy.</p><h2 id="special-note-on-average-ratio-and-distinct-count" tabindex="-1"><a class="header-anchor" href="#special-note-on-average-ratio-and-distinct-count"><span>Special note on average, ratio, and distinct count</span></a></h2><p>Some metrics are not safe to accelerate by storing only one final value.</p><p>Examples:</p><ul><li>average</li><li>percentage</li><li>ratio</li><li>distinct count</li><li>complex calculated measures</li></ul><p>These usually require additional design.</p><p>For example:</p><ul><li>average may require both <code>sum</code> and <code>count</code></li><li>ratio may require numerator and denominator</li><li>distinct count usually cannot be safely re-aggregated by simple addition</li></ul><p>For this reason, aggregation tables are most effective when they focus first on:</p><ul><li><code>GroupBy</code></li><li><code>Sum</code></li><li><code>Count</code></li><li><code>Max</code></li><li><code>Min</code></li></ul><h2 id="troubleshooting" tabindex="-1"><a class="header-anchor" href="#troubleshooting"><span>Troubleshooting</span></a></h2><h3 id="the-aggregation-table-is-configured-but-the-report-still-queries-the-detail-fact-table" tabindex="-1"><a class="header-anchor" href="#the-aggregation-table-is-configured-but-the-report-still-queries-the-detail-fact-table"><span>The aggregation table is configured, but the report still queries the detail fact table</span></a></h3><p>Possible reasons include:</p><ul><li>the report grain is more detailed than the aggregation table</li><li>the report uses dimensions not covered by the aggregation table</li><li>the metric mapping is incorrect</li><li>the summarization method is incorrect</li><li>the report uses unsupported filters</li><li>the aggregation table data is incomplete or outdated</li></ul><h3 id="the-report-result-is-incorrect-after-using-the-aggregation-table" tabindex="-1"><a class="header-anchor" href="#the-report-result-is-incorrect-after-using-the-aggregation-table"><span>The report result is incorrect after using the aggregation table</span></a></h3><p>Possible reasons include:</p><ul><li>the aggregation table measure does not match the intended business metric</li><li>the summarization method was selected incorrectly</li><li>the aggregation grain is inconsistent with the report design</li><li>the aggregation table data is out of sync with the detail table</li></ul><h3 id="i-am-not-sure-whether-to-use-count-or-count-table" tabindex="-1"><a class="header-anchor" href="#i-am-not-sure-whether-to-use-count-or-count-table"><span>I am not sure whether to use Count or Count table</span></a></h3><p>Check the meaning of the aggregation table column in the database.</p><ul><li>If it counts a specific field, use <code>Count</code></li><li>If it counts fact rows rolled into the aggregate row, use <code>Count table</code></li></ul><h2 id="faq" tabindex="-1"><a class="header-anchor" href="#faq"><span>FAQ</span></a></h2><h3 id="can-one-model-use-multiple-aggregation-tables" tabindex="-1"><a class="header-anchor" href="#can-one-model-use-multiple-aggregation-tables"><span>Can one model use multiple aggregation tables</span></a></h3><p>Yes. Different aggregation tables can serve different query patterns, grains, and dashboards.</p><h3 id="can-an-aggregation-table-replace-the-detail-fact-table" tabindex="-1"><a class="header-anchor" href="#can-an-aggregation-table-replace-the-detail-fact-table"><span>Can an aggregation table replace the detail fact table</span></a></h3><p>No. Aggregation tables are used for performance optimization. Detail tables are still needed for detailed analysis and queries that do not match the aggregation table grain.</p><h3 id="how-do-i-know-whether-performance-actually-improved" tabindex="-1"><a class="header-anchor" href="#how-do-i-know-whether-performance-actually-improved"><span>How do I know whether performance actually improved</span></a></h3><p>Compare the following before and after configuration:</p><ul><li>the generated SQL</li><li>the execution time shown in <strong>Execution cost</strong></li><li>the report or dashboard response time</li></ul><h3 id="what-happens-if-the-summarization-method-is-selected-incorrectly" tabindex="-1"><a class="header-anchor" href="#what-happens-if-the-summarization-method-is-selected-incorrectly"><span>What happens if the summarization method is selected incorrectly</span></a></h3><p>The query may not use the aggregation table, or the query result may be incorrect.</p><h2 id="summary" tabindex="-1"><a class="header-anchor" href="#summary"><span>Summary</span></a></h2><p>Aggregation tables in Datafor improve query performance by mapping pre-aggregated data to the detail model.</p><p>To use them effectively, focus on three things:</p><ol><li>design the right aggregation grain</li><li>map dimensions and measures correctly</li><li>verify the generated SQL in the report editor</li></ol><p>If these are done correctly, aggregation tables can significantly improve report performance on large datasets.</p>',223)]))}const u=t(l,[["render",c],["__file","index.html.vue"]]),p=JSON.parse('{"path":"/documentation/Model/Use-Aggregation-Tables/","title":"Use Aggregation Tables","lang":"en-US","frontmatter":{"title":"Use Aggregation Tables","permalink":"/documentation/Model/Use-Aggregation-Tables/","createTime":"2026/04/24 07:37:17"},"headers":[],"readingTime":{"minutes":8.06,"words":2417},"git":{},"filePathRelative":"documentation/Model/60 Use-Aggregation-Tables.md"}');export{u as comp,p as data};
