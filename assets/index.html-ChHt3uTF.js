import{_ as t,c as n,a as r,o}from"./app-DT22nQ-T.js";const s="/assets/llm-assignments-current-D8JqAXzk.png",a="/assets/llm-models-current-CXIkh1MG.png",i="/assets/llm-add-model-current-f6matMIG.png",d="/assets/llm-extra-parameters-current-CKnHaIst.png",l="/assets/llm-switch-scheme-current-DA38DCer.jpg",g={};function c(p,e){return o(),n("div",null,e[0]||(e[0]=[r('<p>LLM configuration has three layers:</p><table><thead><tr><th>Layer</th><th>What it controls</th></tr></thead><tbody><tr><td><strong>Model profile</strong></td><td>Provider connection, model ID, API key, and request parameters.</td></tr><tr><td><strong>Assignment</strong></td><td>The profile used by each Agent stage and by the embedding system role.</td></tr><tr><td><strong>Scheme</strong></td><td>A reusable snapshot of profiles and assignments.</td></tr></tbody></table><h2 id="_1-open-llm-configuration" tabindex="-1"><a class="header-anchor" href="#_1-open-llm-configuration"><span>1. Open LLM configuration</span></a></h2><ol><li>Open <strong>Home → AI Agent</strong>.</li><li>Click <strong>LLM</strong> in the top toolbar.</li></ol><p>The LLM panel is available to administrators in the full AI Agent view. It opens on <strong>Assignments</strong> and shows the current scheme and assignment status.</p><div align="left"><img src="'+s+'" alt="Current LLM assignments" width="100%"></div><p>The current configuration has 14 Agent stages and one <strong>Embedding Model</strong> role. <strong>15/15 assigned</strong> means that every role has a compatible profile and every chat profile has current structured-output verification. It is not a continuous provider-health check.</p><h2 id="_2-create-and-enable-a-model-profile" tabindex="-1"><a class="header-anchor" href="#_2-create-and-enable-a-model-profile"><span>2. Create and enable a model profile</span></a></h2><p>From <strong>Assignments</strong>, click <strong>Manage models →</strong>, then click <strong>Add Model</strong>.</p><div align="left"><img src="'+a+'" alt="Current LLM models" width="100%"></div><div align="left"><img src="'+i+'" alt="Add Model form" width="100%"></div><table><thead><tr><th>Field</th><th>Required</th><th>Description</th></tr></thead><tbody><tr><td><strong>Model Name</strong></td><td>Yes</td><td>Exact provider model ID used at runtime.</td></tr><tr><td><strong>Display name</strong></td><td>No</td><td>Label shown in model lists and assignment selectors.</td></tr><tr><td><strong>Provider</strong></td><td>No</td><td>Endpoint preset and provider identifier. Options are OpenAI, Qwen, DeepSeek, Gemini, Anthropic, and Custom; OpenAI is the default.</td></tr><tr><td><strong>API Endpoint</strong></td><td>Yes</td><td>HTTP(S) provider base endpoint.</td></tr><tr><td><strong>API Key</strong></td><td>To verify</td><td>Required by <strong>Verify and enable</strong>. When editing, leave it blank to keep the saved key.</td></tr><tr><td><strong>Extra Parameters</strong></td><td>No</td><td>Valid JSON object merged into the provider request. The default is <code>{}</code>.</td></tr></tbody></table><p>For Agent stages, use an OpenAI Responses or OpenAI-compatible Chat endpoint. A provider appearing in the menu does not guarantee that its native API is compatible; <strong>Verify and enable</strong> is the acceptance check.</p><p>The form has no capability selector. A new profile is classified as an embedding profile when its name or display name contains <code>embed</code> or <code>embedding</code>; otherwise it is classified as chat. Prefer a supplied scheme or import file when you need an explicitly defined capability.</p><h3 id="correct-activation-order" tabindex="-1"><a class="header-anchor" href="#correct-activation-order"><span>Correct activation order</span></a></h3><ol><li>Click <strong>Create</strong> for a new profile, or <strong>Update</strong> after editing an existing profile.</li><li>Open the saved profile and click <strong>Verify and enable</strong>.</li><li>Wait for the live provider verification to succeed.</li><li>Assign the profile to the required stages.</li></ol><p>Verification of an unsaved draft is not retained. Changing the provider, model ID, endpoint, API key, or effective request parameters clears the previous verification; save the change and run <strong>Verify and enable</strong> again.</p><p>For chat profiles, verification checks real structured output against the provider. It is not a local ping and can make multiple provider calls. Unverified chat profiles cannot be assigned to Agent stages.</p><h2 id="_3-configure-extra-parameters" tabindex="-1"><a class="header-anchor" href="#_3-configure-extra-parameters"><span>3. Configure Extra Parameters</span></a></h2><p>Use <strong>Extra Parameters</strong> only for provider request settings such as <code>temperature</code>, <code>max_output_tokens</code>, <code>top_p</code>, reasoning effort, embedding <code>dimensions</code>, <code>encoding_format</code>, <code>seed</code>, or vendor-specific flags.</p><div align="left"><img src="'+d+'" alt="Extra Parameters help text" width="100%"></div><p>The value must be a JSON object. Do not put credentials, authorization headers, endpoints, <code>response_format</code>, <code>tools</code>, <code>tool_choice</code>, or structured-output controls here; the runtime removes those keys. Do not set both <code>reasoning_effort</code> and <code>thinking_budget</code>.</p><p>Parameter shape depends on the adapter. For example, OpenAI Responses uses <code>{&quot;reasoning&quot;:{&quot;effort&quot;:&quot;medium&quot;}}</code>, while an OpenAI-compatible Chat endpoint may use <code>{&quot;reasoning_effort&quot;:&quot;medium&quot;}</code>.</p><h2 id="_4-manage-model-profiles" tabindex="-1"><a class="header-anchor" href="#_4-manage-model-profiles"><span>4. Manage model profiles</span></a></h2><p>Model cards show the display name, provider model ID, assignment usage, missing-key status, and structured-output verification status.</p><ul><li><strong>Structured output not verified</strong>: save the profile, then run <strong>Verify and enable</strong>.</li><li><strong>Used by <em>n</em> stages</strong>: reassign those stages and save before deleting the profile.</li><li><strong>No API key</strong>: edit the profile and provide a key before verification.</li></ul><p>Saved keys are masked in the UI and API. Leaving the API Key field blank while editing preserves the saved key.</p><h2 id="_5-assign-models-to-runtime-stages" tabindex="-1"><a class="header-anchor" href="#_5-assign-models-to-runtime-stages"><span>5. Assign models to runtime stages</span></a></h2><p>Return to <strong>Assignments</strong>. All 15 rows are currently required.</p><table><thead><tr><th>Assignment</th><th>Runtime key</th><th>Used for</th></tr></thead><tbody><tr><td>Workflow Routing</td><td><code>workflow.routing</code></td><td>Choosing the governed workflow for the request.</td></tr><tr><td>Semantic Binding</td><td><code>semantic.binding</code></td><td>Checking business meaning against model implementation.</td></tr><tr><td>Semantic Draft</td><td><code>semantic.draft</code></td><td>Producing the semantic query draft.</td></tr><tr><td>Analysis Strategy</td><td><code>analysis.strategy</code></td><td>Planning a bounded multi-step investigation.</td></tr><tr><td>Investigation Step Query</td><td><code>analysis.investigation_step_query</code></td><td>Generating the governed query for one investigation step.</td></tr><tr><td>Evidence Analysis</td><td><code>analysis.evidence</code></td><td>Interpreting query results as evidence-backed findings.</td></tr><tr><td>Follow-up Suggestions</td><td><code>analysis.follow_up</code></td><td>Proposing bounded next questions.</td></tr><tr><td>Investigation Observation</td><td><code>analysis.investigation_observation</code></td><td>Normalizing one investigation result into an observation.</td></tr><tr><td>Investigation Replan</td><td><code>analysis.investigation_replan</code></td><td>Deciding whether the investigation plan needs adjustment.</td></tr><tr><td>Investigation Synthesis</td><td><code>analysis.investigation_synthesis</code></td><td>Producing the final answer from investigation evidence.</td></tr><tr><td>Model Brief</td><td><code>model.brief</code></td><td>Summarizing model metadata and analytical capabilities.</td></tr><tr><td>Dashboard Review</td><td><code>dashboard.review</code></td><td>Analyzing the supplied dashboard context.</td></tr><tr><td>Question Suggestions</td><td><code>model.question_suggestions</code></td><td>Generating model-grounded sample questions.</td></tr><tr><td>Failure Explanation</td><td><code>workflow.failure_explanation</code></td><td>Wording a failure that the workflow has already decided.</td></tr><tr><td>Embedding Model</td><td><code>retrieval_embedding</code></td><td>Vector search, metadata retrieval, and semantic matching.</td></tr></tbody></table><p>Agent-stage selectors list verified chat profiles. The <strong>Embedding Model</strong> selector lists embedding profiles. A profile can be reused across multiple stages.</p><p>Click the <strong>Save</strong> button at the top of <strong>Assignments</strong> after any change. Assignment changes are not saved automatically. Saved assignments are read for each subsequent stage call and do not require a service restart. A required stage does not fall back to an old model ID or an unassigned spare profile.</p><h2 id="_6-use-schemes" tabindex="-1"><a class="header-anchor" href="#_6-use-schemes"><span>6. Use schemes</span></a></h2><p><strong>Current scheme</strong> records the template that was last applied or saved. Later manual assignment changes do not rename it, so the scheme name is not proof that the live configuration still matches the template.</p><div align="left"><img src="'+l+'" alt="Switch scheme page with built-in and custom templates and file import" width="100%"></div><p>Use the controls as follows:</p><ul><li>Top <strong>Save</strong>: saves the live stage and system-role assignments.</li><li><strong>Save scheme → Save current configuration</strong>: saves the current profiles and assignments as a custom template.</li><li><strong>Save scheme → Export to file</strong>: downloads the current profiles and resolvable assignments as JSON.</li><li><strong>Switch scheme</strong>: applies a built-in or custom template, or imports a previously exported JSON file.</li></ul><p>Save live assignments before saving a scheme. Schemes and exports never contain API keys.</p><p>When applying a scheme, review the stage changes, enter the provider API key, and change the shared endpoint only if the deployment uses a gateway or proxy. Datafor creates or overwrites profiles referenced by the scheme, verifies them, and writes assignments last; unrelated profiles are untouched. If verification is incomplete, assignments can remain pending. Return to <strong>Assignments</strong>, resolve any warnings, confirm <strong>15/15 assigned</strong>, and click <strong>Save</strong>.</p><p>After importing, add the missing API keys and run <strong>Verify and enable</strong> on the saved profiles before saving assignments.</p><p>If a scheme changes the <strong>Embedding Model</strong>, rebuild the vector index for every data model immediately. An index built with the previous embedding model is not compatible with the new one.</p><h2 id="_7-verify-the-configuration" tabindex="-1"><a class="header-anchor" href="#_7-verify-the-configuration"><span>7. Verify the configuration</span></a></h2><ol><li>Confirm <strong>15/15 assigned</strong> with no verification warning.</li><li>Click the top <strong>Save</strong> button.</li><li>Open <strong>New Chat</strong>, select an analysis model, and confirm that its brief and suggested questions load.</li><li>Run a representative analytical question that exercises routing, query generation, and answer synthesis.</li></ol><table><thead><tr><th>Symptom</th><th>Action</th></tr></thead><tbody><tr><td>A model is absent from an Agent-stage selector</td><td>Confirm it is a chat profile, save it, and run <strong>Verify and enable</strong>.</td></tr><tr><td><strong>Structured output not verified</strong></td><td>Check provider access, endpoint, API key, model ID, and parameters; then verify the saved profile again.</td></tr><tr><td>The Assignments <strong>Save</strong> button is disabled</td><td>Complete all 15 rows and resolve every compatibility or verification warning.</td></tr><tr><td>A previously assigned stage becomes unavailable after a profile edit</td><td>Save the profile and rerun <strong>Verify and enable</strong>; verification is tied to the effective runtime configuration.</td></tr><tr><td>Imported assignments remain pending</td><td>Add API keys, verify imported chat profiles, review all roles, then click <strong>Save</strong>.</td></tr><tr><td>Retrieval fails after changing the Embedding Model</td><td>Rebuild every model&#39;s vector index with the new embedding profile.</td></tr></tbody></table>',44)]))}const m=t(g,[["render",c],["__file","index.html.vue"]]),u=JSON.parse('{"path":"/documentation/AI-Agent/LLM-Configuration/","title":"LLM Configuration","lang":"en-US","frontmatter":{"title":"LLM Configuration","permalink":"/documentation/AI-Agent/LLM-Configuration/","createTime":"2025/08/11 16:44:13"},"headers":[],"readingTime":{"minutes":4.43,"words":1330},"git":{},"filePathRelative":"documentation/AI-Agent/10-LLM-Configuration.md"}');export{m as comp,u as data};
